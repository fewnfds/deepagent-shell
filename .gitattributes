# Vite hashes source content, so frontend inputs and committed output must use
# the same line endings on Windows and Linux checkouts.
frontend/** text=auto eol=lf
server/src/agent_shell/frontend_dist/** text=auto eol=lf
